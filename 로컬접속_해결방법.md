# 로컬 접속 문제 해결 방법

## 문제 상황
리포지토리가 여전히 235MB로 크기 때문에 로컬에서 clone/pull이 제대로 되지 않는 상황입니다.

## 해결 방법 (아래 순서대로 실행)

### 방법 1: 새로 Clone하기 (권장)

기존 로컬 리포지토리를 삭제하고 새로 clone하세요:

```bash
# 1. 기존 디렉토리 백업 (필요한 경우)
mv freqtrade-develop freqtrade-develop-backup

# 2. 새로 clone
git clone --depth 1 https://github.com/kimgeondo/freqtrade-develop.git
cd freqtrade-develop

# 3. 브랜치 체크아웃
git fetch origin copilot/reduce-file-size-under-100mb
git checkout copilot/reduce-file-size-under-100mb
```

### 방법 2: 기존 리포지토리 리셋하기

기존 리포지토리가 있다면:

```bash
cd freqtrade-develop

# 1. 모든 변경사항 저장 (필요한 경우)
git stash

# 2. 원격 브랜치 상태로 강제 리셋
git fetch origin copilot/reduce-file-size-under-100mb
git reset --hard origin/copilot/reduce-file-size-under-100mb

# 3. 저장한 변경사항 복원 (필요한 경우)
git stash pop
```

### 방법 3: Shallow Clone (가장 빠른 방법)

전체 히스토리 없이 최신 상태만 받기:

```bash
git clone --depth 1 --branch copilot/reduce-file-size-under-100mb https://github.com/kimgeondo/freqtrade-develop.git
cd freqtrade-develop
```

## ⚠️ 중요 사항

### Git 히스토리 문제
현재 .git 디렉토리가 189MB로 큰 이유:
- 삭제된 대용량 파일들이 git 히스토리에 남아있음
- 완전히 제거하려면 **force push**가 필요함

### Force Push 실행 (관리자만)

리포지토리를 완전히 정리하려면:

```bash
# 이 명령어는 신중하게 실행하세요!
# 팀원들과 협의 후 실행하세요!
git push --force origin copilot/reduce-file-size-under-100mb
```

**주의**: Force push 후 모든 팀원이 리포지토리를 다시 clone해야 합니다!

## 현재 리포지토리 상태

- **전체 크기**: 235MB
- **.git 크기**: 189MB  
- **작업 파일**: 46MB

## 추가 도움말

### Clone이 계속 실패한다면:

1. **Git 설정 증가**:
```bash
git config --global http.postBuffer 524288000
git config --global http.maxRequestBuffer 100M
git config --global core.compression 0
```

2. **부분적으로 clone**:
```bash
git clone --depth 1 https://github.com/kimgeondo/freqtrade-develop.git
cd freqtrade-develop
git fetch --unshallow
```

3. **SSH 사용해보기**:
```bash
git clone git@github.com:kimgeondo/freqtrade-develop.git
```

## 문제가 계속되면

다음 정보를 알려주세요:
1. 어떤 에러 메시지가 나오는지
2. 어떤 명령어를 실행했는지
3. Git 버전 (`git --version`)
4. 네트워크 환경 (회사망, 집, 카페 등)

---
*작성일: 2025-12-08*
*문의: @copilot*
